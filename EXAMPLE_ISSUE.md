Title: Replace default Vite favicon with Sonica logo

Body:
The application is currently using the default Vite favicon (`/vite.svg`). 

**Task:**
- Create a custom favicon for Sonica (e.g., a musical note icon).
- Add the new icon to `frontend/public/favicon.ico` (or `.svg` / `.png`).
- Create the `frontend/public` directory if it doesn't exist.
- Update `frontend/index.html` to link to the new favicon.

**Acceptance Criteria:**
- The browser tab shows the Sonica logo instead of the Vite logo.
